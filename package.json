{
  "name": "fenshu-registration",
  "version": "1.0.0",
  "description": "分数线登记实时系统",
  "private": true,
  "scripts": {
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "build:server": "npm --prefix server install && npm --prefix server run prisma:generate",
    "build": "npm run build:client && npm run build:server",
    "start": "npm --prefix server run prisma:migrate && node server/index.js",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
